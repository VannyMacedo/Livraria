<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-feature android:name="android.hardware.camera.autofocus" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projeto11"
        tools:targetApi="31">

        <!-- MainActivityInformacoes-->
        <activity
            android:name=".MainActivityInformacoes"
            android:exported="false" />

        <!-- MainActivityFavoritos -->
        <activity
            android:name=".MainActivityFavoritos"
            android:exported="false" />

        <!-- MainActivitySacola -->
        <activity
            android:name=".MainActivitySacola"
            android:exported="false" />

        <!-- MainActivityDetalhes4 -->
        <activity
            android:name=".MainActivityDetalhes4"
            android:exported="false" />

        <!-- MainActivityDetalhes3 -->
        <activity
            android:name=".MainActivityDetalhes3"
            android:exported="false" />

        <!-- MainActivityDetalhes2 -->
        <activity
            android:name=".MainActivityDetalhes2"
            android:exported="false" />

        <!-- MainActivityDetalhes -->
        <activity
            android:name=".MainActivityDetalhes"
            android:exported="false" />

        <!-- MainActivityHome -->
        <activity
            android:name=".MainActivityHome"
            android:exported="false" />

        <!-- MainActivityRegister -->
        <activity
            android:name=".MainActivityRegister"
            android:exported="false" />

        <!-- MainActivityRegister -->
        <activity
            android:name=".MainActivityLogin"
            android:exported="false" />

        <!-- MainActivityLogin - Set as Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->

    </application>

</manifest>